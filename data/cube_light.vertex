#version 330
uniform mat4 MVP;

attribute vec4 vPos;
attribute vec4 vColor;
attribute vec4 vNormal;
attribute vec2 vTexture;

out vec4 fColor;

void main() {
    gl_Position = MVP * vPos;
    fColor = vColor;
}
