#version 330
attribute vec4 vPosition;
attribute vec2 vTexture;
attribute mat4 mModelMatrix;

out vec2 fTextCoord;

uniform mat4 P;
uniform mat4 V;

void main() {
    gl_Position = P * V * mModelMatrix * vPosition;
    fTextCoord = vTexture;
}
