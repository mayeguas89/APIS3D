#version 330

attribute vec4 vPos;
attribute vec4 vColor;
attribute vec2 vTexture;
attribute mat4 mModelMatrix;

out vec4 fColor;
out vec2 fTextCoord;

uniform mat4 P;
uniform mat4 V;

void main() {
    gl_Position = P * V * mModelMatrix * vPos;
    fColor = vColor;
    fTextCoord = vTexture;
}
